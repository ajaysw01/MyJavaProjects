package aj.questions;

import java.util.ArrayList;
import java.util.stream.Collectors;
class Bus {
	
}

public class BusProblem {
		
	public static void main(String[] args) {
		ArrayList<Bus> bus = new ArrayList<Bus>();
		ArrayList<Bus> b = new ArrayList<Bus>();
		
		b = bus.stream().filter(ifp<234).collect(Collectors.toList());
		return b , p
	}

}
